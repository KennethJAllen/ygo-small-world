[project]
name = "ygo-small-world"
version = "1.2.1"
description = "A bridge finder for the card 'Small World' in the card game 'Yu-Gi-Oh!'."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.0",
    "networkx>=3.4.2",
    "numpy>=2.2.1",
    "pandas>=2.2.3",
    "pillow>=11.1.0",
    "pyprojroot>=0.3.0",
    "requests>=2.32.3",
    "streamlit>=1.41.1",
]

[project.scripts]
sw = "ygo_small_world.cli:cli"
update = "ygo_small_world.update_data:update_card_data"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ygo_small_world"]

[tool.hatch.build.targets.wheel.force-include]
"data/cardinfo.csv" = "ygo_small_world/data/cardinfo.csv"

[dependency-groups]
dev = [
    "jupyterlab>=4.4.10",
    "pytest>=8.4.2",
]
